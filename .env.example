# Copy this file to .env and fill in your Supabase project credentials.
# Until these are set, the app runs entirely on local mock data + IndexedDB
# cache (see src/lib/supabaseClient.ts), no code changes needed to switch.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
